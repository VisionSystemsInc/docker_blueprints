version: '2.3'

services:

  gdal_3_2_3:
    build:
      context: ..
      dockerfile: blueprint_gdal.Dockerfile
      # cache_from:
        # - vsiri/blueprint:gdal
        # - vsiri/ci_cache_blueprints:gdal_stage_base
        # - vsiri/ci_cache_blueprints:gdal_stage_openjpeg
        # - vsiri/ci_cache_blueprints:gdal_stage_ecw
        # - vsiri/ci_cache_blueprints:gdal_stage_tiff
        # - vsiri/ci_cache_blueprints:gdal_stage_proj
        # - vsiri/ci_cache_blueprints:gdal_stage_geotiff
        # - vsiri/ci_cache_blueprints:gdal_final
      args:
        GDAL_VERSION: "3.2.3"
    image: &gdal_image vsiri/blueprint_test:gdal-3.2.3

  # pdal_2_2_0:
  #   build:
  #     context: ..
  #     dockerfile: blueprint_pdal.Dockerfile
  #     cache_from:
  #       - vsiri/blueprint:pdal
  #     args:
  #       GDAL_IMAGE: *gdal_image
  #       PDAL_VERSION: "2.2.0"
  #   image: &pdal_image vsiri/blueprint_test:pdal-2.2.0

  # test_gdal:
  #   build:
  #     context: .
  #     dockerfile: test_gdal.Dockerfile
  #     args:
  #       GDAL_IMAGE: *gdal_image
  #   image: vsiri/blueprint_test:test_gdal

  # test_pdal:
  #   build:
  #     context: .
  #     dockerfile: test_pdal.Dockerfile
  #   args:
  #     GDAL_IMAGE: *gdal_image
  #     PDAL_IMAGE: *pdal_image
  #   image: vsiri/blueprint_test:test_pdal
